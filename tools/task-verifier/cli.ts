import { driftCheck, loadAndValidateSpecification } from '../prd-compiler/compiler.js';
import { verifyCoverage, verifyHarness, verifyTask } from './verify.js';

const command = process.argv[2] ?? 'harness';
const option = (name: string): string | undefined => { const index = process.argv.indexOf(name); return index >= 0 ? process.argv[index + 1] : undefined; };
try { const targetWorktree = option('--target-worktree'); const result = command === 'spec:verify' ? await loadAndValidateSpecification().then((spec) => ({ sourceHashes: spec.hashes, counts: { requirements: spec.manifest.requirements.length, acceptanceCriteria: spec.manifest.acceptanceCriteria.length } })) : command === 'coverage' ? await verifyCoverage() : command === 'task' ? await verifyTask(process.argv[3] ?? '', option('--holder') ?? process.env.USER ?? 'local-agent', Number(option('--lease-version')), { trustedRoot: process.cwd(), ...(targetWorktree ? { targetWorktree } : {}) }) : command === 'drift' ? await driftCheck() : await verifyHarness(); console.log(JSON.stringify({ command, status: 'PASS', result }, null, 2)); } catch (error) { console.error(JSON.stringify({ command, status: 'FAIL', error: error instanceof Error ? error.message : String(error) })); process.exitCode = 1; }
