# Independent final product audit

Read `factory/constitution.md`, the full authoritative documentation set, product map, all converged milestone artifacts,
current main, verification, and runtime evidence. Return only JSON conforming to `factory/schemas/final-audit.schema.json`.
An empty work queue is not evidence of convergence. If any requirement, architecture, test, runtime, security, or
operational gap remains, return NOT_CONVERGED with concrete remediation evidence.
