# Provider rights records

Future provider tasks must record official contract version, plan, permitted operations, redistribution rights, jurisdiction, cost class, verification time and expiry. Bootstrap adapters are deterministic and make no live calls.
