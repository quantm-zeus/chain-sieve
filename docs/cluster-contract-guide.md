# Cluster contract guide

A cluster is the independently reviewable integration unit for one dependency group. Its ZCode goal sequences multiple small tasks but preserves per-task leases, verification, and commits. Exit requires deterministic cluster checks and Codex review.
