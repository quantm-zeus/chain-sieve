# ZCode execution guide

Give ZCode the generated `.zcode-goal.md` for the first READY cluster. It must validate hashes, acquire the next task, consume only its context pack, implement and verify it, self-review, make one commit, and stop at every contract stop condition. It must not batch unverified task changes.
