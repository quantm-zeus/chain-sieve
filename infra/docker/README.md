# Local containers

From the repository root run `docker compose up -d`, then apply the SQL migrations with the documented migration command. PostgreSQL listens on 5432; MinIO listens on 9000 and its console on 9001. Credentials in the example are development-only.
