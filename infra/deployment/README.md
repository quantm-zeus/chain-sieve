# Deployment assets

The Dockerfiles provide an immutable baseline for the API and dashboard. A platform-specific production manifest is deliberately deferred until infrastructure, authentication, secret-store, recovery-class, and public-activation requirements receive their generated implementation tasks.
