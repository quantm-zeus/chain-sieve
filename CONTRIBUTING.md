# Contributing

Use exact Node.js 22.23.1 and pnpm 10.13.1. Start from `main` or the stable reviewed base. Compile contracts, select a READY task, acquire its lease, and follow its context pack and `AGENTS.md`. Keep commits atomic and conventional. Never modify generated files directly. A clean contribution passes the complete verification command set in `AGENTS.md`.
