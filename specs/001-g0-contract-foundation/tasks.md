# G0 work packages

- [ ] `g0-domain-contracts` — canonical identity, independent state and temporal contracts.
- [ ] `g0-security-perimeter` — permanent read-only, MCP fail-closed and untrusted-input controls. Depends on domain.
- [ ] `g0-collector-boundary` — bounded collector, registry, checkpoint/gap/reorg behavior. Depends on domain.
- [ ] `g0-tool-core` — authorization, execution envelope, cache/single-flight/quota. Depends on domain and security.
- [ ] `g0-integration-evidence` — persistence/API/MCP walking skeleton and conformance. Depends on all preceding outcomes.

Convergence appends stable-ID remediation packages below this line; it never invokes Spec Kit implementation in the
production loop.

## Convergence

No convergence pass has run against an integrated milestone yet.
